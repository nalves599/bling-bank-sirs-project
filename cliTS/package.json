{
  "name": "blingbank-cli",
  "version": "1.0.0",
  "description": "BlingBank SIRS CLI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\""
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3"
  }
}
